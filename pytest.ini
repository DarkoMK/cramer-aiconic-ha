[pytest]
asyncio_mode = auto
testpaths = tests
# The HA harness imports the integration as custom_components.cramer_aiconic,
# so the repository root has to be importable however pytest is invoked.
pythonpath = .
